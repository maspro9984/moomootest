# moomoo OpenAPI Python SDK（moomoo証券の正式SDK。import名は `moomoo`）
# moomoo OpenD ゲートウェイに接続してリアルタイム相場データを取得するために使用します。
# インストール: pip install -r requirements.txt
# ※ 旧 futu-api でも動作します（コードは両対応）
moomoo-api>=9.0

# Web サーバー
Flask>=3.0.0

# 日本株版(jp_app.py)がRSSPilotのdataapi(WebSocket)に接続するために使用します。
websockets>=12.0
